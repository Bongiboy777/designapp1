import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["cdn.builder.io","images.unsplash.com", "plus.unsplash.com"],
  },
};

export default nextConfig;
